<mvc:View controllerName="QM_Dashboard.controller.MasterAct" xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc">
	<Page id="page" title="{i18n>masterActTitle}" showNavButton="true" navButtonPress="onNavBack">
		<!--		<subHeader>
			<Bar id="searchBar">
				<contentMiddle>
					<SearchField
						id="searchField"
						showRefreshButton="{device>/isNoTouch}"
						search="onSearch"
						tooltip="{i18n>masterSearchTooltip}"
						width="100%">
					</SearchField>
				</contentMiddle>
			</Bar>
		</subHeader>-->
		<content>
			<List id="list" items="{project>/ProjectActivities}" mode="SingleSelectMaster" selectionChange="onSelect" noDataText="{i18n>masterListNoDataText}" swipe="onSwipe" growing="true"
				growingScrollToLoad="true" modeAnimationOn="false" includeItemInSelection="true">
				<items>
					<ObjectListItem type="Active" press="onSelect" title="{project>ProjActTyp}"></ObjectListItem>
				</items>
			</List>
		</content>
		<!--		<footer>
			<Bar>
				<contentRight>
					<Button
						icon="sap-icon://add"
						tooltip="{i18n>masterFooterAddButtonTooltip}"
						press="onAddProduct" />
				</contentRight>
			</Bar>
		</footer>-->
	</Page>
</mvc:View>